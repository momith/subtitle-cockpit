# Environment Configuration for Subtitle Processing System
# Copy this file to .env and adjust the values as needed

# Media directory to mount into the container
# Adjust this to point to your media files location
# Use ABSOLUTE path on HOST machine
MEDIA_DIR=/media/

# Space-separated list of language codes to download for OCR (e.g. "eng deu fra"). Use "all" to download everything.
TESSDATA_LANGS=eng deu

# Mullvad VPN configuration directory (optional)
# If you use VPN configs with API keys, set this to the directory containing .conf files
# Use ABSOLUTE path on HOST machine
MULLVAD_VPN_DIR=/media/mullvad_wireguard_linux_all

# Port to expose the web interface (default: 5000)
APP_PORT=5000

# Flask environment (production or development)
FLASK_ENV=development
FLASK_DEBUG=1

# OCR runtime configuration (used by the container at runtime)
# Number of parallel OCR workers (threads launching separate tesseract processes)
OCR_WORKERS=1
# Tesseract subprocess timeout in seconds (increase if you see timeouts)
TESSERACT_TIMEOUT_SECONDS=120

# Tesseract tessdata download (optional, used at docker build time)
# Set to 1/true to download traineddata files from the official tessdata repo into the container.
TESSDATA_DOWNLOAD=1
# Override raw base URL if needed (advanced)
TESSDATA_REPO_RAW_BASE=https://raw.githubusercontent.com/tesseract-ocr/tessdata/main
